apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: uploads-claim
  namespace: {{ include "hc.namespace" . }}
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: managed-premium
  resources:
    requests:
      # 10G == 6/day == 180/month, target < 1/day
      storage: 1Gi